# Direct dependencies for the radiosonde burst classification v2 pipeline.
# pip install -r requirements.txt   (resolves transitive deps automatically)
# For a fully pinned lockfile, use uv.lock (uv sync) instead.

eccodes>=2.46.0         # BUFR radiosonde file parsing
flask>=3.1.3            # web app
joblib>=1.5.3           # save/load .joblib model artifacts
metpy>=1.7.1            # CAPE/CIN/LCL parcel calculations
numpy>=2.4.3            # array math
openpyxl>=3.1.5         # read Label_End_of_radiosonde.xlsx
pandas>=3.0.1           # data manipulation
scikit-learn>=1.8.0     # RandomForest, StandardScaler, GridSearchCV
shap>=0.52.0            # per-prediction SHAP explanations
